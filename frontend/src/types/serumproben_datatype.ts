export type serumprobenData = {
    barcode_id?: string;
    patient_Id_intern?: string;
    created_at: string;	
    probenart?: string; 	
    boxnummer?: number; 
    boxzeile?: number; 
    boxspalte?: number; 	
    lagerraum?: string;	
    anmerkungen?: string;	
}
